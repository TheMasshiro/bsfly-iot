// ==================== CREDENTIALS TEMPLATE ====================
// This is a TEMPLATE file - rename to credentials.h and fill in your credentials
// DO NOT COMMIT credentials.h TO VERSION CONTROL
// This file is in .gitignore to prevent accidental key leaks

#ifndef CREDENTIALS_H
#define CREDENTIALS_H

// Backend Configuration
const char* BACKEND_URL = "backend link here";

// MQTT Configuration (EMQX)
const char* MQTT_BROKER = "mqtt broker here";
const uint16_t MQTT_PORT = mqtt port;
const char* MQTT_USER = "your-mqtt-username";
const char* MQTT_PASS = "your-mqtt-password";

#endif
